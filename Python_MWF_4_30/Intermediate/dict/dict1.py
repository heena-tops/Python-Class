'''
    Dictionary : Collection in python that can have elements in
                key and value pair

    representation : {}
    in-built class : dict()

    characteristics :

    dicts are indexible(they are indexible by kley only)
    dicts are mutable/ changable
    dicts are iterable
    dict keys has to be any int or string value (it can accept float aslo as key)
    dict keys are distinct/unique
    in terms of value it can duplicate or belog to any data type
'''

# blank declaration

d1={}
d2=dict()

print(type(d1))
print(type(d2))
